import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  postContainer: {
    paddingTop: "4rem",
  },
  cardContainerStyle: {
    padding: "1rem",
  },
}));

export default useStyles;
